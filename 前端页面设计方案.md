# 个人云端记账本 — 前端页面设计方案

> 本文档由 grill-me 交互式设计审查生成，所有关键分支已与用户达成共识。

---

## 1. 技术栈与版本

| 层 | 选型 | 版本 | 备注 |
|---|---|---|---|
| 框架 | Vue 3（Composition API `<script setup>`） | 3.5.x | |
| 语言 | TypeScript | 5.x | |
| 构建 | Vite | 5.x | History 路由模式 + Nginx try_files 兜底 |
| UI 库 | Element Plus | 2.x | `<el-config-provider>` 预留明暗模式切换 |
| 状态 | Pinia + pinia-plugin-persistedstate | 2.x | **accessToken 纯内存，不持久化** |
| HTTP | Axios | 1.x | `withCredentials: true` 携带 HttpOnly Cookie |
| 图表 | ECharts | 5.x | 按需引入折线/饼图 |
| 初始化方式 | `npm create vite@latest ledger-web -- --template vue-ts` | | |

---

## 2. 目录结构

```
ledger-web/
├── public/
├── src/
│   ├── api/              # 按后端模块拆分
│   │   ├── user.ts       # register/login/logout/info/changePassword/delete ↔ UserController
│   │   ├── auth.ts       # refresh ↔ AuthController
│   │   ├── account.ts    # add/page/update/delete ↔ AccountController
│   │   ├── budget.ts     # add/list ↔ BudgetController
│   │   ├── statistics.ts # dashboard ↔ DashboardController
│   │   └── export.ts     # excel/status/download ↔ ExportController
│   ├── views/
│   │   ├── LoginView.vue              # /login 登录+注册 tab
│   │   ├── layout/
│   │   │   └── BasicLayout.vue        # 左侧菜单 + 顶栏
│   │   ├── DashboardView.vue          # /dashboard（首页）
│   │   ├── AccountView.vue            # /account（账目列表+增删改弹窗+导出按钮集成）
│   │   ├── BudgetView.vue             # /budget（预算管理）
│   │   └── NotFoundView.vue           # /404（路由守卫 whiteList 引用，必须存在）
│   ├── stores/
│   │   ├── user.ts                    # { accessToken, userInfo, loading }
│   │   └── app.ts                     # { isDark, sidebarCollapse, activeMenu }
│   ├── router/
│   │   ├── index.ts                   # createWebHistory()
│   │   └── guards.ts                  # beforeEach 路由守卫
│   ├── utils/
│   │   ├── request.ts                 # Axios 实例 + 拦截器 + 刷新重放队列
│   │   ├── auth.ts                    # token 工具 + blob 下载
│   │   ├── date.ts                    # LocalDate 互转
│   │   └── format.ts                  # 金额千分位、分类图标映射
│   ├── tests/                         # vitest（工具函数单测 + 组件冒烟）
│   │   ├── request.spec.ts            # 拦截器 / 401 刷新重放队列
│   │   ├── auth.spec.ts               # token 工具 + blob 下载 + 文件名解析
│   │   ├── date.spec.ts               # LocalDate 互转
│   │   └── format.spec.ts             # 金额千分位、分类映射
│   ├── components/
│   │   ├── AccountEditorDialog.vue    # 账目增改弹窗（方案B）
│   │   ├── CategoryIconGrid.vue       # 分类图标九宫格
│   │   ├── ModifyPasswordDialog.vue   # 修改密码弹窗（旧密码+新密码+确认）
│   │   ├── ConfirmPasswordDialog.vue  # 注销账号密码确认
│   │   └── AmountQuickPad.vue         # 快捷面额块
│   ├── types/
│   │   ├── api.ts                     # Result<T> + 所有 DTO/VO 1:1 对应后端
│   │   └── business.ts                # CategoryDict + TypeEnum
│   ├── assets/                        # 留空，后续 UI/UX 智能体填插画/Logo
│   ├── App.vue                        # el-config-provider + router-view
│   └── main.ts                        # createApp → pinia → router → ElementPlus
├── .env.development                   # VITE_API_BASE_URL=/api
├── .env.production                    # VITE_API_BASE_URL=（空，走同域 Nginx 反代）
├── vite.config.ts                     # server.proxy /api → http://localhost:8080
├── tsconfig.json
└── package.json
```

---

## 3. 页面清单（5 页 + 1 布局组件）

### 3.1 登录/注册页 — `/login`

- **布局**：左品牌占位（灰蓝渐变 + Logo 文字，等 UI/UX 替换）；右 `el-tabs`（登录/注册切换）
- **风格**：参考淘宝/京东简洁大气，大留白，主色按钮
- **登录表单**：账号 + 密码 +「记住我」checkbox（勾选 → refreshToken Cookie 设 `Max-Age=7天`；不勾选 → session Cookie，关浏览器即过期）
- **注册表单**：用户名 + 密码 + 确认密码（**无邮箱**：后端注册接口仅收 `{ username, password }`，user 表无 email 列）
- **校验**：Element Plus form 校验规则

### 3.2 仪表盘 — `/dashboard`（首页）

```
┌─────────────────────────────────────────────┐
│  顶部 4 卡片（el-card + el-statistic）       │
│  本月收入 │ 本月支出 │ 本月结余 │ 预算剩余    │
├─────────────────────┬───────────────────────┤
│  左下                │  右下                  │
│  ECharts 折线图      │  上：ECharts 饼图      │
│  7 日支出趋势        │  分类支出占比          │
│  (trend7Days)       │  下：预算进度条列表    │
│                     │  (budgetProgress)     │
│                     │  >100% 行红标 danger  │
└─────────────────────┴───────────────────────┘
```

### 3.3 账目管理 — `/account`

- **筛选栏**（紧凑一行 + 展开高级筛选）：
  - 默认：日期范围(daterange) + 收支类型(select) + 关键词(input) + 查询/重置
  - 展开高级筛选：分类 select
- **操作按钮**：「+记一笔」「导出 Excel」
- **表格**：el-table + el-pagination
  - 列：日期 / 收支类型(彩色Tag) / 分类(带图标) / 金额(负数红) / 备注 / 创建时间 / 操作(编辑/删除)
  - 删除前 `el-popconfirm` 确认
- **记账弹窗**（AccountEditorDialog.vue）：
  - 顶部 Tab：支出/收入切换
  - 中部：大金额输入框 + 快捷面额块（¥10 ¥20 ¥50 ¥100 ¥500）
  - 下部：分类九宫格图标（餐饮/交通/购物/娱乐/住房/医疗/教育/工资/理财/其他）
  - 底部：日期选择 + 备注 input
  - **金额校验**：`> 0`、最多两位小数（与后端 `decimal(10,2)` 一致）
  - **乐观锁**：编辑模式持有 `AccountVO.version`，PUT /update 时随请求体回传（防并发覆盖）
  - **跨月修改二次确认**：编辑模式下，若新日期月份 ≠ 原日期月份 → 弹 `ElMessageBox.confirm('跨月修改将重置本月及上月的预算进度缓存与仪表盘缓存，是否继续？')` → 确认才 PUT /update
- **导出流程**：
  - 点击导出 → 弹窗选条件 → POST /export/excel
  - 返回 taskId → 轮询 GET /export/status/{taskId} 直到 `status===2`（已完成，状态枚举见 §6.7）
    - `status===3`（失败）→ `ElMessage.error(task.errorMsg)`，终止轮询
    - `status===4`（已过期）→ `ElMessage.warning('文件已过期，请重新导出')`，终止轮询
  - `status===2` 后触发 GET /export/download/{taskId} 用 `<a href download>` 下载
  - 返回"同步完成" → responseType blob 直接下载

### 3.4 预算管理 — `/budget`

- **顶栏**：月份选择器（el-date-picker type="month"）+「设预算」按钮
- **中部卡片网格**：每分类一张 card
  - 分类图标 + 名称
  - ElProgress 进度条（颜色动态：<60% 绿，<90% 黄，≥100% 红）
  - 当前金额 / 预算金额
  - 超支时卡片 border-left 红条 + ElTag type=danger
- **空态**：el-empty + 引导去「设预算」
- **设预算 Dialog**：
  - 当月各分类列表（收入类 disabled = 禁用）
  - 每行 input 填预算金额
  - 保存 → POST /api/budget/add（支持重复覆盖）

### 3.5 BasicLayout.vue（布局组件）

```
┌──────────────────────────────────────────────────────┐
│  el-container                                         │
│  ┌──────┬────────────────────────────────────────┐  │
│  │el-as │  el-header(48~56px)                     │  │
│  │ide   │  面包屑 + 右：暗明开关 + 用户头像下拉    │  │
│  │220px │  ┌────────────────────────────────────┐│  │
│  │可折叠│  │el-main(router-view)                ││  │
│  │      │  │  Dashboard / Account / Budget      ││  │
│  │菜单：│  └────────────────────────────────────┘│  │
│  │📊仪表│                                        │  │
│  │盘    │  el-footer：版权（可选）               │  │
│  │📒账目│                                        │  │
│  │管理  │                                        │  │
│  │💰预算│                                        │  │
│  │管理  │                                        │  │
│  └──────┴────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
```

- 顶栏右侧：
  - 主题切换 el-switch（isDark）
  - 用户头像下拉菜单：修改密码 / 注销账号 / 退出登录（「个人信息」已移除，userInfo 内容不足以支撑独立页面）
  - **修改密码** → ModifyPasswordDialog（旧密码+新密码+确认新密码）→ 成功后清空 Pinia → 跳 /login（改密触发 token_version 自增，当前会话一并作废）
  - **注销账号** → ConfirmPasswordDialog（输入密码二次确认）→ 成功后清空 Pinia 跳 /login

### 3.6 404 页 — `/404`

- `NotFoundView.vue` + 路由 `{ path: '/404', component: NotFoundView }` + 兜底 `{ path: '/:pathMatch(.*)*', redirect: '/404' }`
- 守卫 whiteList 引用 `/404`（§4.3），该页必须存在，否则守卫跳转落空
- 内容：`el-result`（404）+「返回仪表盘」按钮（`router.push('/dashboard')`）

---

## 4. 核心交互规则（必写）

### 4.1 Token 存储策略

| Token | 存储位置 | 说明 |
|---|---|---|
| accessToken | **Pinia 内存（不持久化）** | 防 XSS 持久窃取；页面刷新后通过 refresh 重新获取 |
| refreshToken | **后端 HttpOnly Cookie** | 前端不读写；Axios `withCredentials: true` 自动携带 |

> **「记住我」语义**（§3.1 登录表单）：勾选 → 后端设置 refreshToken Cookie 时带 `Max-Age=7天`（跨浏览器重启保持登录）；不勾选 → session Cookie（关闭浏览器即过期）。两种情况下 refreshToken JWT 内部均限 7 天有效，语义自洽。

### 4.2 Axios 拦截器（utils/request.ts）

**请求拦截器：**
- 若 Pinia.user.accessToken 存在 → `config.headers.Authorization = Bearer ${accessToken}`
- `config.withCredentials = true`（携带 HttpOnly Cookie）

**响应拦截器：**
- 正常 2xx + `Result.code === 200` → `return Promise.resolve(data.data)`
- `code === 2002`（乐观锁冲突）→ `ElMessage.error('该账目已被其他操作修改，请刷新页面重试')` → reject
- `code === 500/2001/其他业务错误` → `ElMessage.error(data.msg)` → reject
- **HTTP 401 或 `code === 401`**（无感刷新）：
  1. 若已有 refresh 请求在飞 → 当前失败请求塞进 pending 队列，返回新 Promise
  2. 若无锁 → 加锁 → `POST /api/auth/refresh`（带 Cookie，不需 body）
     - 成功 → 更新 Pinia accessToken → 重放 pending 队列所有请求（用新 token）
     - 失败 → 清空 Pinia → `ElMessage.warning('登录已过期，请重新登录')` → `router.push('/login?redirect=' + 当前path)` → 队列全部 reject
  3. 解锁

**下载类接口特殊处理：**
- `responseType: 'blob'` 的请求 → 不进 code 拦截器，返回响应对象本身
- `parseFilenameFromDisposition(header)` 解析 `filename*=utf-8''...` 中文文件名

### 4.3 路由守卫（router/guards.ts）

```
beforeEach(to, from, next):
  1. whiteList = ['/login', '/404']，命中 → next()
  2. Pinia.user.accessToken 存在 → 已登录访问 /login 则 next('/dashboard')，其他 next()
  3. accessToken 不存在（刷新/首次）→ 调 authApi.refreshToken()
     - 成功 → 写入 Pinia → next(to.path)
     - 失败 → next('/login?redirect=' + encodeURIComponent(to.fullPath))
  4. 登录成功后从 redirect query 取值跳转
```

### 4.4 跨月修改二次确认

- 编辑模式下，保存前对比 `原 accountDate.month` 与 `新 accountDate.month`（跨年也算）
- 不同 → `ElMessageBox.confirm('跨月修改将重置本月及上月的预算进度缓存与仪表盘缓存，是否继续？', '提示', {type:'warning'})` → 确认才 PUT /update

### 4.5 修改密码（新增功能，依赖后端补接口）

- 入口：顶栏头像下拉 → 「修改密码」→ ModifyPasswordDialog
- 表单字段：旧密码 / 新密码 / 确认新密码（新密码强度复用注册规则：6-100 位）
- 提交：`PUT /api/user/password`，body `{ oldPassword, newPassword }`（后端校验旧密码，失败返回错误码 1006）
- 成功后后端 `token_version` 自增 → 所有旧 Token（含当前会话）失效
- 前端收到成功后：清空 Pinia user store → `ElMessage.success('密码已修改，请重新登录')` → `router.push('/login')`
- **跨端依赖**：该接口后端目前不存在，需后端在 HLD/详细设计中补 U-XX 并实现，否则无法联调（见 §8 风险表）

---

## 5. 环境变量

| 文件 | 变量 | 值 | 说明 |
|---|---|---|---|
| `.env.development` | VITE_API_BASE_URL | `/api` | 结合 `vite.config.ts` 的 `server.proxy./api.target = http://localhost:8080; changeOrigin: true`，本地无跨域 |
| `.env.production` | VITE_API_BASE_URL | `""`（空） | 与前端同域部署，Nginx 反代 `/api` 到后端容器 |

> **说明**：不写死 `http://localhost:8080` 是因为生产环境前端与后端不同域时会导致 Cookie SameSite 问题。开发走 proxy `/api`，生产走同域 Nginx 反代，是业界最佳实践。

---

## 6. 后端接口字段对应（types/api.ts）

### 6.1 通用响应

```typescript
interface Result<T> {
  code: number;      // 200=成功, 401=未授权, 2001=业务错误, 2002=乐观锁冲突
  msg: string;
  data: T;
}
```

### 6.2 用户模块

| 接口 | 方法 | 路径 | 请求体 | 响应体 |
|---|---|---|---|---|
| 注册 | POST | /api/user/register | `{ username, password }` | void |
| 登录 | POST | /api/user/login | `{ username, password }` | `{ accessToken, userInfo: { id, username, nickname } }` |
| 获取信息 | GET | /api/user/info | — | `{ id, username, nickname }` |
| 修改密码 | PUT | /api/user/password | `{ oldPassword, newPassword }` | void |
| 注销账号 | DELETE | /api/user/delete | `{ password }` | void |
| 退出登录 | POST | /api/user/logout | — | void |

> **契约对齐说明**：user 表无 email 列，注册接口仅 `{ username, password }`；`tokenVersion`（后端 `token_version`）为后端内部字段，前端无需也不应持有，故 userInfo 不含该字段。

### 6.3 认证模块

| 接口 | 方法 | 路径 | 请求体 | 响应体 |
|---|---|---|---|---|
| 刷新Token | POST | /api/auth/refresh | —（Cookie自动携带） | `{ accessToken, userInfo }` |

### 6.4 账目模块

| 接口 | 方法 | 路径 | 请求体 | 响应体 |
|---|---|---|---|---|
| 新增 | POST | /api/account/add | `{ type:0\|1, category, amount, accountDate, remark?, extraJson? }` | `Long` |
| 分页 | POST | /api/account/page | `{ pageNum, pageSize, type?, category?, startDate?, endDate?, keyword? }` | `{ records: AccountVO[], total, pageNum, pageSize }` |
| 修改 | PUT | /api/account/update | `{ id, type, category, amount, accountDate, remark?, version }` | void |
| 删除 | DELETE | /api/account/delete/{id} | — | void |

**AccountVO**: `{ id, type:0\|1, category, amount:BigDecimal, accountDate:LocalDate, remark, version, createTime, updateTime }`

### 6.5 预算模块

| 接口 | 方法 | 路径 | 请求体 | 响应体 |
|---|---|---|---|---|
| 设定 | POST | /api/budget/add | `{ budgets: [{ category, amount }], month: "YYYY-MM" }` | void |
| 查询 | GET | /api/budget/list?month=YYYY-MM | — | `BudgetVO[]` |

**BudgetVO**: `{ category, budgetAmount, currentAmount, percent, status }`

### 6.6 统计模块

| 接口 | 方法 | 路径 | 请求体 | 响应体 |
|---|---|---|---|---|
| 仪表盘 | GET | /api/statistics/dashboard | — | `DashboardResponse` |

**DashboardResponse**: `{ monthSummary: { income, expense, balance }, categoryBreakdown: [{ category, amount }], trend7Days: [{ date, expense }], budgetProgress: [{ category, budgetAmount, currentAmount, percent }] }`

### 6.7 导出模块

| 接口 | 方法 | 路径 | 请求体 | 响应体 |
|---|---|---|---|---|
| Excel导出 | POST | /api/export/excel | `{ type?, category?, startDate?, endDate? }` | `String`（taskId 或 "同步导出已完成"） |
| 查询状态 | GET | /api/export/status/{taskId} | — | `ExportTaskVO` |
| 下载文件 | GET | /api/export/download/{taskId} | — | `Blob`（文件流） |

**ExportTaskVO**: `{ taskId, status:0\|1\|2\|3\|4, rowCount, fileSize, fileName, errorMsg, createTime }`

> **状态语义**（与后端 export_task 表一一对应）：0-待处理，1-处理中，**2-已完成（可下载）**，3-失败（展示 `errorMsg`），4-已过期（需重新导出）。字段使用 `rowCount/fileSize/errorMsg`，后端无 `processedRows` 字段；`fileName` 取自后端 `file_url` 的文件名部分。

---

## 7. 分类字典（types/business.ts）

与后端测试数据保持一致：

```typescript
// 支出分类
const EXPENSE_CATEGORIES = [
  '餐饮', '交通', '购物', '娱乐', '住房', '医疗', '教育', '其他'
] as const;

// 收入分类
const INCOME_CATEGORIES = [
  '工资', '理财', '礼金', '其他'
] as const;

// Type 枚举
enum AccountType {
  EXPENSE = 0,
  INCOME = 1
}
```

---

## 8. 风险与假设

| 项 | 内容 | 建议 |
|---|---|---|
| CORS/HttpOnly Cookie | 开发环境前端 5173、后端 8080，refreshToken 需 Set-Cookie | 强制开发走 `server.proxy /api`；后端 CorsConfig 开发配置 `allowed-origin-patterns: http://localhost:5173` + `allow-credentials: true` |
| 分类字典一致性 | 后端 category 是纯 String，前端字典名必须与历史数据一致 | `types/business.ts` 字典与接口测试文档 §2.5 A-01 测试数据保持一致 |
| 401 双重处理 | 后端 401 可能既走 Result.code=401 又走真实 HTTP 401 | `utils/request.ts` 写独立 `handleUnauthorized()`，两处都调用；保证不触发二次 refresh 死循环 |
| 明暗切换仅预留钩子 | 仅引入 dark/css-vars.css + isDark store + config-provider class | 不提前写死 `--el-color-primary`，让 UI/UX 智能体后续填充 |
| 下载中文文件名 | Content-Disposition: `filename*=utf-8''%E8%B4%A6...` | `utils/auth.ts` 提供 `parseFilenameFromDisposition(header)` 用 decodeURIComponent + RFC 5987 正则解析 |
| 修改密码接口缺失（跨端） | 顶栏菜单已承诺该功能，但后端无 `PUT /api/user/password`，无法联调 | **后端待办**：HLD/详细设计补接口（校验旧密码 1006、成功自增 token_version），前端 `ModifyPasswordDialog` 与之联调；改密成功会使当前会话登出 |
| userInfo 契约（跨端） | 后端 user 表无 email 列，前端旧契约 `userInfo.email/userVersion` 不存在 | **后端待办**：/api/user/info 返回 `{ id, username, nickname }`；前端 types 已按此对齐，联调前须后端确认 |
| 导出契约（跨端） | 后端 export_task 无 `processedRows`，字段为 `row_count/file_size/error_msg`；状态 3=失败、4=过期 | **后端待办**：ExportTaskVO 按 `{ taskId, status, rowCount, fileSize, fileName, errorMsg, createTime }` 返回；前端轮询 `status===2` 才下载 |

---

## 9. 被舍弃的方案

- 独立「系统设置」页 → 改为头像下拉调用 Dialog（减少一页，贴合 4-5 页需求）
- 独立「导出管理」页 → 集成到账目页顶栏（需求明确要求）
- localStorage 持久化 accessToken → 安全优先，纯内存 + 无感刷新
- 路由 Hash 模式 → 选择 History（后续部署 Nginx try_files）

---

## 10. 开发步骤建议

1. **脚手架搭建**：`create vite` + 装依赖 + 目录结构 + env + vite proxy + ElementPlus + Pinia + VueRouter 空骨架跑起来
2. **TypeScript 类型层**：`types/api.ts` 1:1 对应后端所有 DTO/VO + Result<T> 泛型；`types/business.ts` 分类字典 + type 枚举
3. **utils/request.ts**：拦截器三件套（请求带 Bearer + Cookie withCredentials；401 重放队列；blob 下载；错误 code 统一处理）
4. **stores/user.ts + router/guards.ts**：路由守卫刷新时调 refresh
5. **api/* 6 个模块**：axios 实例调后端接口，入参出参严格类型
6. **登录页 LoginView.vue**：tab 切 注册/登录 → 调接口 → 成功写 Pinia → 跳 redirect；注册无邮箱字段；「记住我」勾选态随登录请求传给后端控制 Cookie 持久性
7. **BasicLayout.vue**：左侧菜单 + 头像下拉 + 暗明切换
8. **DashboardView + ECharts**：4 卡 + 折线 + 饼 + 预算进度条
9. **AccountView + AccountEditorDialog + CategoryIconGrid + 导出流程**（最大块）
10. **BudgetView**：月份 + 预算卡片网格 + 设预算 Dialog + 空态
11. **注销账号 + 修改密码 Dialog**：头像下拉调用，密码二次确认；改密成功清空 Pinia 跳 /login（依赖后端补接口）
12. **联调测试**：注册→登录→仪表盘→记账→分页→修改跨月→删除→设预算→导出→刷新→改密(被登出)→重新登录→退出→全流程验证
13. **前端测试（vitest）**：工具函数单测（request 拦截器/401 刷新重放队列、auth blob 下载+文件名解析、date 互转、format 千分位）+ 关键组件冒烟（登录表单校验、记账弹窗、跨月确认）

---

## 11. Element Plus 组件清单

| 用途 | 组件 |
|---|---|
| 布局 | el-container, el-aside, el-header, el-main, el-footer |
| 导航 | el-menu, el-breadcrumb, el-dropdown |
| 表单 | el-form, el-form-item, el-input, el-select, el-option, el-date-picker, el-radio, el-switch, el-checkbox |
| 数据展示 | el-table, el-table-column, el-pagination, el-card, el-tag, el-progress, el-statistic, el-empty |
| 反馈 | el-dialog, el-drawer, el-message, el-message-box, el-popconfirm, el-loading, el-result（404 页） |
| 其他 | el-tabs, el-tab-pane, el-config-provider, el-avatar, el-divider |
