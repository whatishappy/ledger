# 个人记账本 Ledger — 环境变量示例
# 复制此文件为 .env（或通过 IDE Run Configuration / Docker -e / PowerShell $env: 注入）。
# .env 已被 .gitignore 忽略，请勿提交真实密钥。
#
# 配置模式说明：
# 项目保持 dev / prod / application.yml 三层配置，密钥一律通过环境变量注入。
# AI Key 未设置时，对应占位符解析为空串，AI 模块自动进入降级模式（接口返回 6011）。

# ============== 启动必需 ==============
# JWT 签名密钥（必须设置，否则应用拒绝启动）
# 生成方式（PowerShell）：
#   [byte[]]$b=New-Object byte[] 64; [System.Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($b); [Convert]::ToBase64String($b)
JWT_SECRET=

# ============== AI 联调（可选，缺失时 AI 接口返回 6011） ==============
# 主模型
AI_PRIMARY_API_KEY=
# 备用模型
AI_BACKUP_API_KEY=
# Embedding 向量模型
AI_EMBEDDING_API_KEY=

# ============== 可选（有默认值） ==============
# MySQL 密码（默认 123456）
# MYSQL_PASSWORD=
# Redis 密码（默认 123456）
# REDIS_PASSWORD=
# MinIO 凭据（默认 minioadmin/minioadmin）
# MINIO_ACCESS_KEY=
# MINIO_SECRET_KEY=
